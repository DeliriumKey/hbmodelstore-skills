# 功能支持

本模型公开两项有界能力：按代码或名称搜索基金份额，以及把精确份额代码解析为初始基金代码。
搜索单次最多 20 条，包含历史退出基金，不提供全表下载、存续状态、分类或模型覆盖判断。

完整工作流与命令见[基金搜索与主份额解析](./fund-search.md)。公开状态以 Skill 根目录
[`capabilities.json`](../../../capabilities.json) 的 `fund-resolution` 为准。
